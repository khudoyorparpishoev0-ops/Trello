/**
 * Граница рабочего пространства.
 *
 * Мультиарендности в CORE пока нет: доска хранится одной записью. Но строка
 * «default» не должна расползтись по SQL и сервисам — иначе, когда появится
 * второе пространство, её придётся выискивать по всему коду. Поэтому значение
 * объявлено один раз здесь, а методы репозиториев и сервисов обязаны принимать
 * `workspaceId` параметром, даже если сегодня он всегда один и тот же.
 */
export const DEFAULT_WORKSPACE_ID = 'default'

/** Идентификатор пространства. Отдельный тип, чтобы его не путали с board id. */
export type WorkspaceId = string
