/**
 * Логическая модель данных.
 *
 * Сама модель живёт в общем коде (`shared/domain/types.ts`) — она нужна и
 * серверу. Здесь только переэкспорт, чтобы импорты `@/types` в компонентах
 * остались прежними.
 */
export * from '#shared/domain/types'
